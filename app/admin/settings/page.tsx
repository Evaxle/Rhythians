"use client";

import { useEffect, useState } from "react";
import { Pencil, Trash2 } from "lucide-react";
import { MessageSettings } from "@/components/admin/message-settings";

interface UserOption { userId: string; globalRank: number | null; user: { username: string; displayName: string | null } }
interface Entry { id: string; cameraMode: string; userId: string; settingsFileName: string; settingsFileUrl?: string; videoUrl?: string; username: string; displayName: string | null; globalRank: number | null; title: string | null; description: string | null }

const videoAccept = "video/*,.mp4,.webm,.mov,.m4v,.mkv,.avi,.wmv,.flv,.mpeg,.mpg,.3gp,.ogv,.ts,.mts,.m2ts";

export default function AdminSettingsPage() {
  const [users, setUsers] = useState<UserOption[]>([]); const [entries, setEntries] = useState<Entry[]>([]); const [query, setQuery] = useState(""); const [mode, setMode] = useState("lock"); const [userId, setUserId] = useState(""); const [settingsFile, setSettingsFile] = useState<File | null>(null); const [videoFile, setVideoFile] = useState<File | null>(null); const [title, setTitle] = useState(""); const [description, setDescription] = useState(""); const [editingId, setEditingId] = useState(""); const [busy, setBusy] = useState(false); const [error, setError] = useState("");

  async function load() { const response = await fetch("/api/admin/settings", { cache: "no-store" }); if (response.ok) setEntries((await response.json()).settings ?? []); }
  async function searchUsers(value: string) { setQuery(value); const response = await fetch(`/api/admin/settings/users?q=${encodeURIComponent(value)}`, { cache: "no-store" }); if (response.ok) setUsers((await response.json()).users ?? []); }
  useEffect(() => { void load(); void searchUsers(""); }, []);

  function editEntry(entry: Entry) { setEditingId(entry.id); setMode(entry.cameraMode); setUserId(entry.userId); setTitle(entry.title ?? ""); setDescription(entry.description ?? ""); setSettingsFile(null); setVideoFile(null); window.scrollTo({ top: 0, behavior: "smooth" }); }
  function resetForm() { setEditingId(""); setMode("lock"); setUserId(""); setSettingsFile(null); setVideoFile(null); setTitle(""); setDescription(""); setError(""); }

  async function save() {
    setError("");
    if (!userId || (!editingId && (!settingsFile || !videoFile))) { setError(editingId ? "Choose a connected Rhythia user." : "Choose a connected Rhythia user, settings file, and video."); return; }
    if (settingsFile && !/\.rhs$/i.test(settingsFile.name)) { setError("Settings file must be an .rhs file."); return; }
    setBusy(true);
    try {
      let uploadData: any = {};
      if (settingsFile || videoFile) {
        if (!settingsFile || !videoFile) throw new Error("When replacing files, select both the settings file and video.");
        const prepare = await fetch("/api/admin/settings", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "upload", settingsFileName: settingsFile.name, settingsFileSize: settingsFile.size, settingsContentType: "application/octet-stream", videoFileName: videoFile.name, videoFileSize: videoFile.size, videoContentType: videoFile.type || "application/octet-stream" }) });
        uploadData = await prepare.json(); if (!prepare.ok) throw new Error(uploadData.error ?? "Could not prepare uploads.");
        const [settingsUpload, videoUpload] = await Promise.all([fetch(uploadData.settingsUploadUrl, { method: "PUT", headers: { "Content-Type": "application/octet-stream" }, body: settingsFile }), fetch(uploadData.videoUploadUrl, { method: "PUT", headers: { "Content-Type": videoFile.type || "application/octet-stream" }, body: videoFile })]);
        if (!settingsUpload.ok || !videoUpload.ok) { const failed = !settingsUpload.ok && !videoUpload.ok ? "settings file and video" : !settingsUpload.ok ? "settings file" : "video"; throw new Error(`The ${failed} failed to upload.`); }
      }
      const action = editingId ? "update" : "create";
      const payload = { action, id: editingId || undefined, cameraMode: mode, userId, settingsPath: uploadData.settingsPath, videoPath: uploadData.videoPath, settingsFileName: settingsFile?.name, title, description };
      const response = await fetch("/api/admin/settings", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) });
      const data = await response.json(); if (!response.ok) throw new Error(data.error ?? "Could not save settings.");
      resetForm(); await load();
    } catch (submitError) { setError(submitError instanceof Error ? submitError.message : "Could not save settings."); } finally { setBusy(false); }
  }

  async function remove(id: string) { if (!confirm("Delete this community settings entry?")) return; await fetch("/api/admin/settings", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "delete", id }) }); if (editingId === id) resetForm(); await load(); }

  return <div className="space-y-6"><div className="rounded-3xl border border-border bg-surface/95 p-8 shadow-glow"><h1 className="text-3xl font-semibold text-white">System settings</h1><p className="mt-3 text-sm leading-7 text-muted">Configure branding, open graph metadata, feature flags, Discord integration, and player settings showcases.</p></div><section className="rounded-3xl border border-border bg-surface/95 p-7 shadow-glow"><p className="text-xs font-bold uppercase tracking-[0.3em] text-accent">Community Settings Showcase</p><h2 className="mt-2 text-2xl font-black text-white">{editingId ? "Edit community settings" : "Add community settings"}</h2><p className="mt-2 text-sm text-muted">Add or edit the user, camera mode, title, description, settings file, and gameplay video shown publicly.</p><div className="mt-6 grid gap-5 lg:grid-cols-2"><div><label className="text-xs font-bold uppercase tracking-[0.2em] text-muted">Camera mode</label><div className="mt-2 grid grid-cols-2 gap-2"><button onClick={() => setMode("lock")} className={`rounded-xl px-4 py-3 font-bold ${mode === "lock" ? "bg-accent text-white" : "border border-border text-muted"}`}>Lock</button><button onClick={() => setMode("spin")} className={`rounded-xl px-4 py-3 font-bold ${mode === "spin" ? "bg-accent text-white" : "border border-border text-muted"}`}>Spin</button></div></div><div><label className="text-xs font-bold uppercase tracking-[0.2em] text-muted">Connected Rhythia user</label><input value={query} onChange={(e) => void searchUsers(e.target.value)} placeholder="Search username..." className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-white outline-none focus:border-accent" /><select value={userId} onChange={(e) => setUserId(e.target.value)} className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-white"><option value="">Choose user</option>{users.map((user) => <option key={user.userId} value={user.userId}>{user.user.displayName || user.user.username} · {user.globalRank ? `#${user.globalRank}` : "Unranked"}</option>)}</select></div><div><label className="text-xs font-bold uppercase tracking-[0.2em] text-muted">Settings file {editingId && "(optional replacement)"}</label><p className="mt-1 text-xs text-muted">Accepts .rhs files only.</p><input type="file" accept=".rhs,application/octet-stream" onChange={(e) => setSettingsFile(e.target.files?.[0] ?? null)} className="mt-2 block w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-white" /></div><div><label className="text-xs font-bold uppercase tracking-[0.2em] text-muted">Gameplay video {editingId && "(optional replacement)"}</label><p className="mt-1 text-xs text-muted">Accepts standard video formats including MP4, WebM, MOV, MKV, AVI, and more.</p><input type="file" accept={videoAccept} onChange={(e) => setVideoFile(e.target.files?.[0] ?? null)} className="mt-2 block w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-white" /></div><div><label className="text-xs font-bold uppercase tracking-[0.2em] text-muted">Title</label><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Optional title" className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-white" /></div><div><label className="text-xs font-bold uppercase tracking-[0.2em] text-muted">Description</label><textarea value={description} onChange={(e) => setDescription(e.target.value)} rows={3} placeholder="Optional player/settings information" className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm text-white" /></div></div>{editingId && <p className="mt-4 rounded-xl border border-accent/20 bg-accent/10 p-3 text-sm text-accent">Editing an existing entry. Leave both file inputs empty to keep the current RHS and video.</p>}{error && <p className="mt-4 rounded-xl border border-red-400/30 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<div className="mt-5 flex gap-2"><button disabled={busy} onClick={() => void save()} className="rounded-xl bg-accent px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{busy ? "Saving..." : editingId ? "Save changes" : "Publish settings"}</button>{editingId && <button disabled={busy} onClick={resetForm} className="rounded-xl border border-border px-5 py-3 text-sm font-semibold text-muted hover:text-white">Cancel edit</button>}</div></section><section className="space-y-3"><div className="flex items-center justify-between"><h2 className="text-lg font-bold text-white">Published community settings</h2><span className="text-sm text-muted">{entries.length} entries</span></div>{entries.map((entry) => <div key={entry.id} className="flex flex-col gap-4 rounded-2xl border border-border bg-surface/90 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><p className="font-bold text-white">{entry.displayName || entry.username} · {entry.cameraMode}</p><p className="truncate text-sm text-muted">{entry.globalRank ? `Global #${entry.globalRank}` : "Unranked"} · {entry.settingsFileName}{entry.title ? ` · ${entry.title}` : ""}</p></div><div className="flex shrink-0 gap-2"><button onClick={() => editEntry(entry)} className="inline-flex items-center gap-2 rounded-xl border border-accent/30 px-3 py-2 text-sm font-semibold text-accent"><Pencil size={15} /> Edit</button><button onClick={() => void remove(entry.id)} className="inline-flex items-center gap-2 rounded-xl border border-red-400/30 px-3 py-2 text-sm font-semibold text-red-200"><Trash2 size={15} /> Delete</button></div></div>)}</section><MessageSettings /></div>;
}
