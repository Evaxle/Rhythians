import { MapsClientLayout } from "@/components/maps/maps-client-layout";

export default function MapsLayout({ children }: { children: React.ReactNode }) {
  return <MapsClientLayout>{children}</MapsClientLayout>;
}
