import Link from "next/link";
import { CircleUserRound, Link2, LogIn, Users } from "lucide-react";
import { getSessionUser } from "@/lib/auth";
import { prisma } from "@/lib/db";
import { getOnlineGlobalRankedUsers } from "@/lib/online-users";
import { getRankInfo } from "@/lib/ranks";
import { getAvatarUrl } from "@/lib/avatar";

export const dynamic = "force-dynamic";

export default async function OnlinePage() {
  const user = await getSessionUser();
  if (!user) return <div className="mx-auto w-full max-w-4xl px-4 py-8 sm:px-6"><section className="rounded-3xl border border-border bg-surface/95 p-8 text-center shadow-glow sm:p-10"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-accent/15 text-accent"><LogIn size={26} /></div><h1 className="mt-5 text-2xl font-semibold text-white">Sign in to see online users</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-muted">Online users are available to members with a linked Rhythia account.</p><Link href="/login" className="mt-6 inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3 text-sm font-semibold text-white"><LogIn size={16} /> Sign in</Link></section></div>;

  const profile = await prisma.rhythiaProfile.findUnique({ where: { userId: user.id }, select: { id: true } });
  if (!profile) return <div className="mx-auto w-full max-w-4xl px-4 py-8 sm:px-6"><section className="rounded-3xl border border-border bg-surface/95 p-8 text-center shadow-glow sm:p-10"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-accent/15 text-accent"><Link2 size={26} /></div><h1 className="mt-5 text-2xl font-semibold text-white">Link your Rhythia account</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-muted">Link your Rhythia profile before viewing the online list.</p><Link href={`/profile/${encodeURIComponent(user.profileHandle)}`} className="mt-6 inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3 text-sm font-semibold text-white"><Link2 size={16} /> Go to my profile</Link></section></div>;

  const users = await getOnlineGlobalRankedUsers();

  return <div className="mx-auto w-full max-w-7xl space-y-6 px-4 py-4 sm:px-6 lg:space-y-8 lg:py-6"><section className="rounded-3xl border border-border bg-surface/95 p-5 shadow-glow sm:p-6"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div className="min-w-0"><p className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.22em] text-accent"><Users size={15} /> Online</p><h1 className="mt-2 text-2xl font-semibold text-white sm:text-3xl">Online users</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted">Online players sorted by their place on the global ranked leaderboard. Their Challenge Level is shown beside their ranked tier.</p></div><span className="inline-flex w-fit shrink-0 items-center rounded-full border border-emerald-400/30 bg-emerald-400/10 px-3 py-1.5 text-xs font-semibold text-emerald-200">{users.length} online</span></div></section>{users.length === 0 ? <section className="rounded-3xl border border-border bg-surface/95 p-10 text-center shadow-glow"><CircleUserRound className="mx-auto text-muted" size={32} /><p className="mt-4 text-sm text-muted">No users are currently online.</p></section> : <section className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5">{users.map((onlineUser) => { const rank = getRankInfo(onlineUser.rhp); const avatarUrl = getAvatarUrl(onlineUser, 128); return <Link key={onlineUser.id} href={`/profile/${encodeURIComponent(onlineUser.profileHandle)}`} className="group min-w-0 rounded-3xl border border-border bg-surface/95 p-4 shadow-glow transition hover:border-accent/30 hover:bg-white/[0.03]"><div className="flex min-w-0 items-start justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><div className="relative shrink-0"><div className="h-12 w-12 overflow-hidden rounded-full border border-border bg-background">{avatarUrl ? <img src={avatarUrl} alt={onlineUser.displayName ?? onlineUser.username} className="h-full w-full object-cover" /> : <div className="flex h-full w-full items-center justify-center text-muted"><CircleUserRound size={24} /></div>}</div><span className="absolute bottom-0 right-0 h-3.5 w-3.5 rounded-full border-2 border-surface bg-emerald-400" /></div><div className="min-w-0"><p className="truncate text-sm font-semibold text-white group-hover:text-accent">{onlineUser.displayName ?? onlineUser.username}</p><p className="mt-1 truncate text-xs text-muted">{onlineUser.title}</p></div></div><span className="shrink-0 text-xs font-bold text-muted">#{onlineUser.globalPosition ?? "-"}</span></div><div className="mt-4 grid min-w-0 grid-cols-1 gap-2 sm:grid-cols-2"><div className="min-w-0 rounded-2xl border border-border bg-background/50 p-3"><p className="text-[10px] uppercase tracking-[0.16em] text-muted">Ranked</p><p className="mt-1 truncate text-sm font-semibold" style={{ color: rank.color }}>{rank.name} {rank.tier}</p></div><div className="min-w-0 rounded-2xl border border-border bg-background/50 p-3"><p className="text-[10px] uppercase tracking-[0.16em] text-muted">Challenge</p><p className="mt-1 truncate text-sm font-semibold text-white">Level {onlineUser.challengeLevel}</p></div></div></Link>; })}</section>}</div>;
}
