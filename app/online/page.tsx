import Link from "next/link";
import { CircleUserRound, Link2, LogIn, Users, Wifi } from "lucide-react";
import { getSessionUser } from "@/lib/auth";
import { prisma } from "@/lib/db";
import { getOnlineGlobalRankedUsers } from "@/lib/online-users";
import { getRankInfo } from "@/lib/ranks";
import { getAvatarUrl } from "@/lib/avatar";
import { RankIcon } from "@/components/rank-icon";

export const dynamic = "force-dynamic";

export default async function OnlinePage() {
  const user = await getSessionUser();
  if (!user) return <div className="ui-page max-w-3xl"><section className="ui-card rounded-[2rem] p-10 text-center shadow-glow"><div className="mx-auto flex h-14 w-14 animate-pulse items-center justify-center rounded-2xl bg-accent/15 text-accent"><LogIn size={26} /></div><h1 className="mt-5 text-2xl font-semibold text-white">Sign in to see online users</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-muted">Online users are available to members with a linked Rhythia account.</p><Link href="/login" className="mt-6 inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3 text-sm font-semibold text-white"><LogIn size={16} /> Sign in</Link></section></div>;
  const profile = await prisma.rhythiaProfile.findUnique({ where: { userId: user.id }, select: { id: true } });
  if (!profile) return <div className="ui-page max-w-3xl"><section className="ui-card rounded-[2rem] p-10 text-center shadow-glow"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-accent/15 text-accent"><Link2 size={26} /></div><h1 className="mt-5 text-2xl font-semibold text-white">Link your Rhythia account</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-muted">Link your Rhythia profile before viewing the online list.</p><Link href={`/profile/${encodeURIComponent(user.profileHandle)}`} className="mt-6 inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3 text-sm font-semibold text-white"><Link2 size={16} /> Go to my profile</Link></section></div>;
  const users = await getOnlineGlobalRankedUsers();
  return <div className="ui-page space-y-6"><section className="relative overflow-hidden rounded-[2rem] border border-white/10 bg-[radial-gradient(circle_at_8%_0%,rgba(85,214,160,0.14),transparent_30%),radial-gradient(circle_at_94%_100%,rgba(124,143,240,0.12),transparent_28%),linear-gradient(145deg,rgba(20,27,45,0.96),rgba(9,14,24,0.98))] p-6 shadow-[0_24px_80px_rgba(0,0,0,.22)] sm:p-8"><div className="relative flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.25em] text-emerald-300"><span className="relative flex h-2.5 w-2.5"><span className="absolute inline-flex h-full w-full animate-ping rounded-full bg-emerald-400 opacity-70" /><span className="relative inline-flex h-2.5 w-2.5 rounded-full bg-emerald-400" /></span><Users size={15} /> Community presence</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.04em] text-white">Who&apos;s online</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-muted">Live Rhythians sorted by global ranked position, with their current rank tier and Challenge Level.</p></div><div className="flex items-center gap-3"><div className="rounded-2xl border border-emerald-400/15 bg-emerald-400/[0.05] px-5 py-3"><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-muted">Active now</p><p className="mt-1 text-3xl font-bold text-white">{users.length}</p></div><div className="hidden h-12 w-12 items-center justify-center rounded-2xl border border-white/10 bg-white/5 text-emerald-300 sm:flex"><Wifi size={21} /></div></div></div></section>{users.length === 0 ? <section className="rounded-[2rem] border border-dashed border-white/10 bg-black/10 p-12 text-center shadow-glow animate-rise-in"><CircleUserRound className="mx-auto text-muted" size={38} /><p className="mt-4 text-sm font-semibold text-white">The community is quiet right now</p><p className="mt-1 text-sm text-muted">No users are currently online.</p></section> : <section className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-3">{users.map((onlineUser, index) => { const rank = getRankInfo(onlineUser.rhp); const avatarUrl = getAvatarUrl(onlineUser, 128); const displayName = onlineUser.displayName ?? onlineUser.username; return <Link key={onlineUser.id} href={`/profile/${encodeURIComponent(onlineUser.profileHandle)}`} className="group relative min-w-0 overflow-hidden rounded-[2rem] border bg-surface/90 p-5 opacity-0 shadow-glow transition duration-300 hover:-translate-y-1 hover:shadow-2xl animate-rise-in" style={{ animationDelay: `${Math.min(index * 45, 500)}ms`, borderColor: `${rank.color}35`, background: `radial-gradient(circle at 100% 0%, ${rank.color}18, transparent 43%), linear-gradient(145deg, rgba(20,27,45,.92), rgba(10,15,25,.96))` }}><div className="absolute right-5 top-5 rounded-full border border-white/10 bg-black/20 px-2.5 py-1 text-[10px] font-bold tabular-nums text-muted">#{onlineUser.globalPosition ?? "-"}</div><div className="flex min-w-0 items-center gap-4"><div className="relative shrink-0"><div className="relative h-16 w-16 overflow-hidden rounded-2xl border-2 bg-background" style={{ borderColor: `${rank.color}70`, boxShadow: `0 0 28px ${rank.color}18` }}>{avatarUrl ? <img src={avatarUrl} alt={displayName} className="h-full w-full object-cover" /> : <div className="flex h-full w-full items-center justify-center text-xl font-bold" style={{ color: rank.color }}>{onlineUser.username.charAt(0).toUpperCase()}</div>}<span className="absolute -bottom-2 -right-2 rounded-xl border border-surface bg-surface p-0.5"><RankIcon rank={rank} size={28} /></span></div><span className="absolute -bottom-1 -left-1 flex h-4 w-4 items-center justify-center rounded-full border-2 border-surface bg-emerald-400"><span className="h-1.5 w-1.5 rounded-full bg-white" /></span></div><div className="min-w-0 pr-8"><p className="truncate text-base font-semibold text-white transition group-hover:text-accent">{displayName}</p><p className="mt-1 truncate text-xs text-muted">{onlineUser.title}</p><p className="mt-2 text-xs font-semibold text-emerald-300">Online now</p></div></div><div className="mt-5 grid grid-cols-2 gap-2"><div className="rounded-2xl border border-white/10 bg-black/15 p-3 transition group-hover:bg-white/[0.04]"><p className="text-[10px] uppercase tracking-[0.16em] text-muted">Ranked</p><div className="mt-1 flex items-center gap-2"><RankIcon rank={rank} size={25} /><p className="truncate text-sm font-semibold" style={{ color: rank.color }}>{rank.isExpert ? "Expert" : `${rank.name} ${rank.tier}`}</p></div></div><div className="rounded-2xl border border-white/10 bg-black/15 p-3 transition group-hover:bg-white/[0.04]"><p className="text-[10px] uppercase tracking-[0.16em] text-muted">Challenge</p><p className="mt-1 truncate text-sm font-semibold text-white">Level {onlineUser.challengeLevel}</p></div></div><div className="mt-4 flex items-center justify-between text-[11px] text-muted"><span>{onlineUser.rhp.toLocaleString()} RHP</span><span className="font-semibold" style={{ color: rank.color }}>#{onlineUser.globalPosition ?? "-"} global</span></div></Link>; })}</section>}</div>;
}
