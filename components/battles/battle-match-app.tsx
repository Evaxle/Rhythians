"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { Check, Download, Loader2, Swords, Trophy, UserRound } from "lucide-react";
import { getRankInfo, rankLabel } from "@/lib/ranks";

function Avatar({ player, large = false }: { player: any; large?: boolean }) { const size = large ? "h-20 w-20" : "h-12 w-12"; return player.avatar ? <img src={player.avatar} alt={player.username} className={`${size} rounded-full border border-border object-cover`} /> : <div className={`${size} flex items-center justify-center rounded-full border border-border bg-accent/10 text-accent`}><UserRound size={large ? 28 : 19} /></div>; }

export function BattleMatchApp({ matchId }: { matchId: string }) {
  const [data, setData] = useState<any>(null); const [loading, setLoading] = useState(true); const [checking, setChecking] = useState(false); const [accepting, setAccepting] = useState(false); const [error, setError] = useState(""); const [countdown, setCountdown] = useState(3);
  async function load() { const response = await fetch(`/api/battles/matches?id=${encodeURIComponent(matchId)}`, { cache: "no-store" }); const result = await response.json(); if (response.ok) setData(result); else setError(result.error ?? "Match not found."); setLoading(false); }
  useEffect(() => { load(); const timer = setInterval(load, 2500); return () => clearInterval(timer); }, [matchId]);
  useEffect(() => { if (!data?.match?.startedAt || data.match.status !== "active") return; setCountdown(3); const timer = setInterval(() => setCountdown((value) => Math.max(0, value - 1)), 1000); return () => clearInterval(timer); }, [data?.match?.startedAt, data?.match?.status]);
  async function checkScore() { setChecking(true); setError(""); const response = await fetch("/api/battles/matches", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "check-score", matchId }) }); const result = await response.json(); setChecking(false); if (!response.ok) { setError(result.error ?? "Could not check your score."); return; } await load(); }
  async function accept() { setAccepting(true); const response = await fetch("/api/battles/matches", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "accept", matchId }) }); const result = await response.json(); setAccepting(false); if (!response.ok) setError(result.error ?? "Could not accept battle."); else await load(); }
  if (loading) return <div className="flex min-h-[60vh] items-center justify-center text-muted"><Loader2 className="mr-2 animate-spin" />Loading battle...</div>;
  if (!data) return <div className="mx-auto max-w-2xl rounded-3xl border border-red-400/20 bg-red-400/10 p-8 text-center text-red-300">{error || "Battle unavailable."}</div>;
  const players = data.players ?? []; const teamOne = players.filter((player: any) => player.team === 1); const teamTwo = players.filter((player: any) => player.team === 2); const mode = String(data.match.mode ?? "1v1").split(":")[0]; const isLarge = mode === "15v15"; const finished = data.match.status === "finished"; const viewer = players.find((player: any) => player.userId === data.viewerId);
  const Team = ({ team, label }: { team: any[]; label: string }) => <div className="flex-1 rounded-3xl border border-border bg-surface/95 p-5"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs uppercase tracking-[0.2em] text-accent">{label}</p><p className="mt-1 text-sm text-muted">{team.length} players</p></div><Trophy className="text-accent" size={20} /></div><div className={isLarge ? "grid grid-cols-3 gap-2" : "space-y-3"}>{team.map((player: any) => { const rank = getRankInfo(player.rhp); const tags = Array.isArray(player.tags) ? player.tags : []; return <Link href={`/profile/${encodeURIComponent(player.profileHandle)}`} key={player.userId} className={`group flex items-center gap-3 rounded-2xl border border-border bg-background/40 p-3 transition hover:border-accent/30 ${isLarge ? "flex-col text-center" : ""}`}><Avatar player={player} large={!isLarge && mode === "1v1"} /><div className="min-w-0 flex-1"><p className="truncate font-semibold text-white group-hover:text-accent">{player.displayName ?? player.username}</p>{!isLarge && <><div className="mt-1 flex flex-wrap gap-1">{tags.map((tag: any) => <span key={tag.slug} className="rounded-full border border-accent/30 bg-accent/10 px-2 py-0.5 text-[10px] font-bold text-accent">{tag.name}</span>)}</div><span className="mt-1 inline-flex rounded-full border border-border px-2 py-0.5 text-[10px] font-semibold" style={{ color: rank.color }}>{rankLabel(rank)}</span></>}{player.accuracy != null && <p className="mt-1 text-xs text-muted">{Number(player.accuracy).toFixed(2)}%</p>}</div></Link>; })}</div></div>;

  return <div className="mx-auto max-w-7xl space-y-5"><section className="rounded-3xl border border-border bg-surface/95 p-6 shadow-glow"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="flex items-center gap-2 text-xs uppercase tracking-[0.22em] text-accent"><Swords size={15} /> {data.match.matchType} battle</p><h1 className="mt-2 text-3xl font-semibold text-white">{mode}</h1><p className="mt-1 text-sm text-muted">{data.match.status === "queue" ? "Searching for opponents..." : data.match.status === "invite" ? "Waiting for the opponent to accept." : finished ? "Battle complete" : "Map battle"}</p></div>{data.match.status === "active" && countdown > 0 && <div className="flex h-14 w-14 items-center justify-center rounded-full border border-accent/40 bg-accent/10 text-2xl font-bold text-accent">{countdown}</div>}</div></section>{data.match.status === "queue" ? <section className="rounded-3xl border border-border bg-surface/95 p-12 text-center shadow-glow"><Loader2 className="mx-auto animate-spin text-accent" size={34} /><h2 className="mt-5 text-xl font-semibold text-white">Finding your battle</h2><p className="mt-2 text-sm text-muted">{data.match.matchType === "ranked" ? "Waiting for players in your exact rank and tier." : "Waiting for players in your rank."}</p></section> : data.match.status === "invite" ? <section className="rounded-3xl border border-border bg-surface/95 p-12 text-center shadow-glow"><Swords className="mx-auto text-accent" size={38} /><h2 className="mt-5 text-2xl font-semibold text-white">1v1 battle request</h2><p className="mt-2 text-sm text-muted">{viewer?.displayName ?? viewer?.username} has invited you to a {data.match.matchType} battle.</p><button onClick={accept} disabled={accepting} className="mt-6 rounded-full bg-accent px-7 py-3 text-sm font-semibold text-white disabled:opacity-50">{accepting ? "Starting..." : "Accept battle"}</button>{error && <p className="mt-4 text-sm text-red-300">{error}</p>}</section> : <><section className="flex flex-col gap-5 lg:flex-row"><Team team={teamOne} label="Team 1" /><div className="flex items-center justify-center px-2 text-4xl font-black italic text-muted">VS</div><Team team={teamTwo} label="Team 2" /></section>{data.map && <section className="rounded-3xl border border-border bg-surface/95 p-6 shadow-glow"><div className="flex flex-wrap items-center gap-5">{data.map.imageUrl && <img src={data.map.imageUrl} alt="" className="h-24 w-24 rounded-2xl object-cover" />}<div className="min-w-0 flex-1"><p className="text-xs uppercase tracking-[0.2em] text-accent">Battle map</p><h2 className="mt-1 text-xl font-semibold text-white">{data.map.title}</h2><p className="mt-1 text-sm text-muted">{data.map.artist ?? ""}{data.map.rating != null ? ` · ${Number(data.map.rating).toFixed(2)}★` : ""}</p></div>{data.map.mapFileUrl && <a href={data.map.mapFileUrl} className="inline-flex items-center gap-2 rounded-full border border-border px-4 py-2.5 text-sm font-semibold text-white hover:border-accent/40"><Download size={16} /> Download map</a>}{!finished && <button disabled={checking || countdown > 0} onClick={checkScore} className="inline-flex items-center gap-2 rounded-full bg-accent px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-40">{checking ? <Loader2 className="animate-spin" size={16} /> : <Check size={16} />} Check score</button>}</div>{error && <p className="mt-4 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-300">{error}</p>}</section>}{finished && <section className="rounded-3xl border border-accent/20 bg-accent/5 p-8 text-center"><Trophy className="mx-auto text-accent" size={34} /><h2 className="mt-4 text-2xl font-semibold text-white">Battle complete</h2><p className="mt-2 text-sm text-muted">Scores have been recorded and ranked rewards have been processed.</p></section>}</>}</div>;
}
