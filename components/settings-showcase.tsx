"use client";

import { useEffect, useState } from "react";
import { Download, Play, Settings2, Trophy } from "lucide-react";
import { RankIcon } from "@/components/rank-icon";
import { getRankInfo } from "@/lib/ranks";

type Entry = { id: string; cameraMode: "lock" | "spin"; settingsFileUrl: string; settingsFileName: string; videoUrl: string; title: string | null; description: string | null; username: string; displayName: string | null; profileHandle: string; avatar: string | null; globalRank: number | null; rhp: number; rhythianRank: string | null; rhythianRankColor: string | null };

export function SettingsShowcase() {
  const [entries, setEntries] = useState<Entry[]>([]);
  const [mode, setMode] = useState<"lock" | "spin">("lock");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  useEffect(() => {
    fetch("/api/settings", { cache: "no-store" }).then(async (response) => {
      if (!response.ok) throw new Error("Unable to load community settings.");
      setEntries((await response.json()).settings ?? []);
    }).catch((reason) => setError(reason instanceof Error ? reason.message : "Unable to load community settings.")).finally(() => setLoading(false));
  }, []);

  const visible = entries.filter((entry) => entry.cameraMode === mode);

  return <div className="mx-auto w-full max-w-7xl space-y-8 px-4 py-5 sm:px-6 lg:py-8"><section className="relative overflow-hidden rounded-[2rem] border border-border bg-surface/95 p-6 shadow-glow sm:p-8"><div className="absolute -right-24 -top-24 h-64 w-64 rounded-full bg-accent/10 blur-3xl" /><div className="relative flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.25em] text-accent"><Settings2 size={15} /> Community settings</p><h1 className="mt-3 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Player settings</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-muted">Browse settings from featured Rhythians, sorted by their live global Rhythia rank.</p></div><div className="flex rounded-2xl border border-border bg-background/60 p-1"><button type="button" onClick={() => setMode("spin")} className={`rounded-xl px-5 py-2.5 text-sm font-semibold transition ${mode === "spin" ? "bg-accent text-white shadow-lg" : "text-muted hover:text-white"}`}>Spin</button><button type="button" onClick={() => setMode("lock")} className={`rounded-xl px-5 py-2.5 text-sm font-semibold transition ${mode === "lock" ? "bg-accent text-white shadow-lg" : "text-muted hover:text-white"}`}>Lock</button></div></div></section>{error && <p className="rounded-xl border border-red-400/30 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}{loading ? <section className="rounded-3xl border border-border bg-surface/70 p-8 text-sm text-muted shadow-glow">Loading player settings...</section> : visible.length === 0 ? <section className="rounded-3xl border border-dashed border-border bg-surface/70 p-12 text-center shadow-glow"><Trophy className="mx-auto text-muted" size={38} /><p className="mt-4 text-sm font-semibold text-white">No {mode === "lock" ? "camera lock" : "camera spin"} settings published yet</p><p className="mt-1 text-sm text-muted">Community settings added by the Rhythians team will appear here.</p></section> : <section className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{visible.map((entry, index) => { const rank = getRankInfo(entry.rhp); return <article key={entry.id} className="group relative min-w-0 overflow-hidden rounded-3xl border bg-surface/95 shadow-glow transition duration-300 hover:-translate-y-1 hover:shadow-2xl" style={{ borderColor: `${rank.color}30`, background: `radial-gradient(circle at 100% 0%, ${rank.color}16, transparent 45%), rgba(15,20,34,0.95)` }}><div className="aspect-video bg-black"><video src={entry.videoUrl} controls preload="metadata" className="h-full w-full object-cover" /></div><div className="p-5"><div className="flex items-center gap-3"><div className="relative shrink-0"><div className="h-14 w-14 overflow-hidden rounded-2xl border-2 bg-background" style={{ borderColor: `${rank.color}70` }}>{entry.avatar ? <img src={entry.avatar} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full place-items-center text-muted"><Trophy size={24} /></div>}</div><span className="absolute -bottom-2 -right-2 rounded-xl border border-surface bg-surface p-0.5"><RankIcon rank={rank} size={28} /></span></div><div className="min-w-0 flex-1 pr-8"><a href={`/profile/${encodeURIComponent(entry.profileHandle)}`} className="block truncate text-base font-semibold text-white hover:text-accent">{entry.displayName ?? entry.username}</a><p className="mt-1 text-xs text-muted">Global #{entry.globalRank ?? "-"} · {entry.rhp.toLocaleString()} RHP</p></div></div><div className="mt-5 rounded-2xl border border-border bg-background/50 p-3"><div className="flex items-center gap-2"><RankIcon rank={rank} size={30} /><div><p className="text-[10px] uppercase tracking-[0.16em] text-muted">Rhythian rank</p><p className="text-sm font-semibold" style={{ color: rank.color }}>{rank.isExpert ? "Expert" : `${rank.name} ${rank.tier}`}</p></div></div></div>{entry.title && <h2 className="mt-4 line-clamp-2 text-lg font-bold text-white">{entry.title}</h2>}{entry.description && <p className="mt-2 line-clamp-3 text-sm leading-6 text-muted">{entry.description}</p>}<div className="mt-4 flex flex-wrap gap-2"><a href={entry.settingsFileUrl} download={entry.settingsFileName} className="inline-flex items-center gap-2 rounded-full bg-accent px-4 py-2 text-sm font-semibold text-white hover:bg-accent2"><Download size={14} /> Download</a><a href={entry.videoUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-border bg-white/5 px-4 py-2 text-sm font-semibold text-white"><Play size={14} /> Video</a></div></div><span className="absolute right-4 top-[calc(56.25%+1rem)] text-xs font-bold tabular-nums" style={{ color: rank.color }}>#{index + 1}</span></article>; })}</section>}</div>;
}
