"use client";
import { useEffect,useState } from "react";
import { Radio } from "lucide-react";
import { getRankInfo } from "@/lib/ranks";
import { UserAvatar } from "@/components/user-avatar";
export function LiveStreamers({title="Live streamers"}:{title?:string}){const[live,setLive]=useState<any[]>([]);useEffect(()=>{let active=true;let loading=false;const load=async()=>{if(loading||document.visibilityState==="hidden")return;loading=true;try{const response=await fetch("/api/streamers/live");const data=await response.json();if(active&&response.ok)setLive(data.live??[]);}catch{}finally{loading=false;}};void load();const timer=window.setInterval(()=>void load(),60_000);const onVisible=()=>{if(document.visibilityState==="visible")void load();};window.addEventListener("focus",onVisible);document.addEventListener("visibilitychange",onVisible);return()=>{active=false;window.clearInterval(timer);window.removeEventListener("focus",onVisible);document.removeEventListener("visibilitychange",onVisible);};},[]);if(!live.length)return null;const groups=new Map<string,any[]>();for(const entry of live){const current=groups.get(entry.userId)??[];current.push(entry);groups.set(entry.userId,current);}const grouped=[...groups.values()].slice(0,5);return <section className="rounded-[2rem] border border-rose-400/15 bg-[linear-gradient(145deg,rgba(244,63,94,.07),rgba(12,17,29,.95))] p-5 shadow-glow sm:p-6"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.22em] text-rose-300"><span className="relative flex h-2.5 w-2.5"><span className="absolute h-full w-full animate-ping rounded-full bg-rose-400 opacity-70"/><span className="relative h-2.5 w-2.5 rounded-full bg-rose-400"/></span><Radio size={15}/>{title}</div><div className="mt-4 flex gap-4 overflow-x-auto pb-1">{grouped.map(accounts=>{const user=accounts[0];const rank=getRankInfo(Number(user.rhp)||0);const avatar=user.avatar&&user.discordId?`https://cdn.discordapp.com/avatars/${user.discordId}/${user.avatar}.png?size=128`:null;const destinations=accounts.map(account=>({platform:account.platform,url:account.liveUrl||account.profileUrl}));return <div key={user.userId} className="min-w-[105px] text-center"><a href={destinations.length===1?destinations[0].url:`/profile/${encodeURIComponent(user.profileHandle)}`} target={destinations.length===1?"_blank":undefined} rel={destinations.length===1?"noreferrer":undefined} className="group relative mx-auto block"><span className="block h-16 w-16 overflow-hidden rounded-full border-[3px] border-rose-500 shadow-[0_0_20px_rgba(244,63,94,.25)]"><UserAvatar src={avatar} username={user.rhythiansUsername} displayName={user.displayName} className="h-full w-full object-cover" fallbackClassName="h-full w-full"/></span><span className="absolute -bottom-1 left-1/2 -translate-x-1/2 rounded-full bg-rose-500 px-2 py-0.5 text-[9px] font-black text-white">LIVE</span></a><p className="mt-3 truncate text-xs font-semibold text-white">{user.displayName??user.rhythiansUsername}</p><p className="mt-0.5 text-[10px]" style={{color:rank.color}}>#{user.globalPosition} global</p>{destinations.length>1&&<div className="mt-1 flex justify-center gap-1">{destinations.map(item=><a key={item.platform} href={item.url} target="_blank" rel="noreferrer" className="rounded-full border border-white/10 bg-white/5 px-2 py-0.5 text-[9px] capitalize text-muted hover:text-white">{item.platform}</a>)}</div>}</div>;})}</div></section>;}
