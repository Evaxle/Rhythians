"use client";

import { useState } from "react";
import { motion } from "motion/react";
import { CheckCircle2, Download, FolderDown, Lock, RefreshCw, Video, GalleryHorizontalEnd } from "lucide-react";
import { CATEGORY_LABELS, MAX_CATEGORY_LEVEL, type Category } from "@/lib/category-constants";
import { CategoryPills } from "@/components/categories/category-pills";

type MapEntry = { id: string; category: Category; level: number; title: string; artist: string | null; description: string | null; mapFileUrl: string; imageUrl: string | null; mapperName: string | null; noteCount: number | null; length: number | null; completion: { passed: boolean; accuracy: number | null } | null };

export function CategoryMapsTab({ category, onCategoryChange, level, maps }: { category: Category; onCategoryChange: (category: Category) => void; level: number; maps: MapEntry[] }) {
  const [busyId, setBusyId] = useState("");
  const [busyLevel, setBusyLevel] = useState<number | null>(null);
  const [messages, setMessages] = useState<Record<string, { tone: "ok" | "warn" | "err"; text: string }>>({});

  async function checkMap(id: string) {
    setBusyId(id);
    setMessages((current) => ({ ...current, [id]: { tone: "ok", text: "Checking your scores..." } }));
    try {
      const response = await fetch("/api/categories/check", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ categoryMapId: id }) });
      const data = await response.json();
      if (!response.ok) throw new Error(data.error ?? "Unable to check your scores.");
      const text = data.status === "level_up" ? `Level up! You reached ${CATEGORY_LABELS[category]} level ${data.level}.` : data.status === "passed" ? "Map completed." : data.status === "already" ? "You already completed this map." : data.status === "locked" ? `Locked — pass a level ${data.requiredLevel} map first.` : "No passing score found yet.";
      setMessages((current) => ({ ...current, [id]: { tone: data.status === "locked" || data.status === "not_beat" ? "warn" : "ok", text } }));
    } catch (error) {
      setMessages((current) => ({ ...current, [id]: { tone: "err", text: error instanceof Error ? error.message : "Unable to reach the server." } }));
    } finally { setBusyId(""); }
  }

  async function checkLevel(mapLevel: number) {
    setBusyLevel(mapLevel);
    setMessages((current) => ({ ...current, [`level-${mapLevel}`]: { tone: "ok", text: `Checking every ${CATEGORY_LABELS[category]} Level ${mapLevel} map...` } }));
    try {
      const response = await fetch("/api/categories/check-level", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ category, level: mapLevel }) });
      const data = await response.json();
      if (!response.ok) throw new Error(data.error ?? "Unable to check this level.");
      setMessages((current) => ({ ...current, [`level-${mapLevel}`]: { tone: "ok", text: `Checked ${data.checked} maps · ${data.passed} new pass${data.passed === 1 ? "" : "es"} · ${data.already} already complete.` } }));
    } catch (error) {
      setMessages((current) => ({ ...current, [`level-${mapLevel}`]: { tone: "err", text: error instanceof Error ? error.message : "Unable to check this level." } }));
    } finally { setBusyLevel(null); }
  }

  const nextLevel = Math.min(MAX_CATEGORY_LEVEL, level + 1);
  const levels = Array.from({ length: MAX_CATEGORY_LEVEL }, (_, i) => i + 1);

  return <div className="space-y-6"><div className="ui-card flex flex-col gap-4 rounded-3xl p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm text-muted">Category: <span className="font-semibold text-white">{CATEGORY_LABELS[category]}</span></p><p className="mt-1 text-sm text-muted">Your level: <span className="font-semibold text-accent">{level}</span>{level < MAX_CATEGORY_LEVEL && <span className="ml-2 text-xs text-muted">Next: Level {nextLevel}</span>}</p></div><div className="flex flex-col items-stretch gap-2 sm:items-end"><CategoryPills selected={category} onSelect={onCategoryChange} /><a href={`/api/categories/download?category=${encodeURIComponent(category)}`} className="inline-flex items-center justify-center gap-2 rounded-full border border-accent/40 bg-accent/10 px-4 py-2 text-sm font-semibold text-white"><FolderDown size={15} /> Download all levels</a></div></div>
    {levels.map((mapLevel) => { const levelMaps = maps.filter((map) => map.level === mapLevel); const isNext = mapLevel === nextLevel; const isLocked = mapLevel > nextLevel; const isDone = mapLevel <= level; const proofLevel = mapLevel >= 7; const levelMessage = messages[`level-${mapLevel}`]; return <motion.section initial={{ opacity: 0, y: 10 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, margin: "-80px" }} key={mapLevel} className="ui-card rounded-3xl p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className={`inline-flex h-9 w-9 items-center justify-center rounded-xl text-sm font-bold ${isDone ? "bg-emerald-400/15 text-emerald-300" : isNext ? "bg-accent/15 text-accent" : "bg-white/5 text-muted"}`}>{mapLevel}</span><div><p className="text-sm font-semibold text-white">Level {mapLevel}</p><p className="text-xs text-muted">{isDone ? "Completed" : isNext ? "Your next level" : "Locked"}</p></div></div><div className="flex flex-wrap items-center gap-2"><span className="text-xs text-muted">{levelMaps.length} map{levelMaps.length === 1 ? "" : "s"}</span>{!proofLevel && !isLocked && <button type="button" onClick={() => void checkLevel(mapLevel)} disabled={busyLevel === mapLevel} className="inline-flex items-center gap-1.5 rounded-full bg-accent px-3 py-1.5 text-xs font-semibold text-white disabled:opacity-50"><RefreshCw size={13} className={busyLevel === mapLevel ? "animate-spin" : ""} /> {busyLevel === mapLevel ? "Checking..." : "Check all level"}</button>}{proofLevel && <a href={`/challenge/clips?kind=category&category=${encodeURIComponent(category)}&level=${mapLevel}`} className="inline-flex items-center gap-1.5 rounded-full border border-accent/30 bg-accent/10 px-3 py-1.5 text-xs font-semibold text-white"><GalleryHorizontalEnd size={13} /> View clips</a>}{proofLevel && !isLocked && mapLevel === level + 1 && <a href={`/completion?kind=category&category=${encodeURIComponent(category)}&level=${mapLevel}`} className="inline-flex items-center gap-1.5 rounded-full bg-accent px-3 py-1.5 text-xs font-semibold text-white"><Video size={13} /> Submit clip</a>}{levelMaps.length > 0 && <a href={`/api/categories/download?category=${encodeURIComponent(category)}&level=${mapLevel}`} className="inline-flex items-center gap-1.5 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-xs font-semibold text-white"><Download size={13} /> Download level</a>}</div></div>{levelMessage && <p className={`mt-3 rounded-xl border p-3 text-xs ${levelMessage.tone === "err" ? "border-red-400/30 bg-red-500/10 text-red-200" : "border-accent/20 bg-accent/5 text-muted"}`}>{levelMessage.text}</p>}{levelMaps.length === 0 ? <p className="mt-4 rounded-2xl border border-dashed border-border bg-background/50 p-4 text-sm text-muted">No maps in this level yet.</p> : <div className="mt-4 grid gap-3 lg:grid-cols-2">{levelMaps.map((map) => { const message = messages[map.id]; const lengthLabel = map.length != null ? `${Math.floor(map.length / 60000)}:${String(Math.round((map.length % 60000) / 1000)).padStart(2, "0")}` : null; return <article key={map.id} className={`flex flex-col rounded-2xl border p-4 ${isLocked ? "border-white/10 bg-black/10 opacity-55" : "border-border bg-background/60 hover:border-accent/20"}`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs text-muted">{map.artist ?? "Unknown artist"}</p><h3 className="mt-1 truncate text-base font-semibold text-white">{map.title}</h3><p className="mt-1 text-xs text-muted">Mapped by {map.mapperName ?? "Unknown"}</p></div>{map.completion?.passed ? <span className="inline-flex shrink-0 items-center gap-1 rounded-full border border-emerald-400/30 bg-emerald-400/10 px-2.5 py-1 text-xs font-semibold text-emerald-300"><CheckCircle2 size={13} /> Done</span> : isLocked ? <span className="inline-flex shrink-0 items-center gap-1 rounded-full border border-border bg-white/5 px-2.5 py-1 text-xs font-semibold text-muted"><Lock size={13} /> Locked</span> : null}</div><div className="mt-3 flex flex-wrap gap-2 text-xs text-muted">{map.noteCount != null && <span className="rounded-full border border-border bg-background/60 px-2.5 py-1">{map.noteCount.toLocaleString()} notes</span>}{lengthLabel && <span className="rounded-full border border-border bg-background/60 px-2.5 py-1">{lengthLabel}</span>}</div><div className="mt-auto flex flex-wrap gap-2 pt-4"><a href={map.mapFileUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-accent/40 bg-accent/10 px-4 py-2 text-sm font-semibold text-white"><Download size={15} /> Download</a>{!proofLevel && <button type="button" onClick={() => void checkMap(map.id)} disabled={busyId === map.id || isLocked} className="inline-flex items-center gap-2 rounded-full bg-accent px-4 py-2 text-sm font-semibold text-white disabled:opacity-50"><RefreshCw size={15} className={busyId === map.id ? "animate-spin" : ""} />{busyId === map.id ? "Checking..." : "Check my score"}</button>}</div>{message && <p className={`mt-3 rounded-xl border p-3 text-sm ${message.tone === "ok" ? "border-emerald-400/30 bg-emerald-400/10 text-emerald-200" : message.tone === "warn" ? "border-amber-400/30 bg-amber-400/10 text-amber-200" : "border-red-400/30 bg-red-500/10 text-red-200"}`}>{message.text}</p>}</article>; })}</div>}</motion.section>; })}</div>;
}
